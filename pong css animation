<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>CSS-Only Pong</title>
    <style>
        body {
            background-color: black;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow: hidden;
        }
        .game-container {
            position: relative;
            width: 600px;
            height: 300px;
            border: 4px solid white;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        .paddle {
            width: 10px;
            height: 80px;
            background-color: white;
            position: absolute;
        }
        .paddle-left {
            left: 10px;
            animation: paddle-move 2s infinite alternate ease-in-out;
        }
        .paddle-right {
            right: 10px;
            animation: paddle-move 2s infinite alternate-reverse ease-in-out;
        }
        .ball {
            width: 15px;
            height: 15px;
            background-color: white;
            position: absolute;
            border-radius: 50%;
            animation: ball-move 3s infinite alternate ease-in-out;
        }
        
        @keyframes ball-move {
            0% { top: 10%; left: 10%; }
            25% { top: 80%; left: 90%; }
            50% { top: 50%; left: 50%; }
            75% { top: 20%; left: 90%; }
            100% { top: 80%; left: 10%; }
        }
        
        @keyframes paddle-move {
            0% { top: 10%; }
            100% { top: 60%; }
        }
    </style>
</head>
<body>
    <div class="game-container">
        <div class="paddle paddle-left"></div>
        <div class="ball"></div>
        <div class="paddle paddle-right"></div>
    </div>
</body>
</html>
